import 'package:flutter/material.dart';

class PageBOld extends StatelessWidget {
  const PageBOld({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          toString(),
        ),
      ),
    );
  }
}
