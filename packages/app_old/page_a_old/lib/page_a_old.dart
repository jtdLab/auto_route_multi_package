import 'package:flutter/material.dart';

class PageAOld extends StatelessWidget {
  const PageAOld({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          toString(),
        ),
      ),
    );
  }
}
